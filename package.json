{
  "name": "server-manager",
  "version": "1.0.0",
  "description": "Server Hosting & Deployment Manager Dashboard",
  "scripts": {
    "install": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "npx concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
